from django.http import JsonResponse

def home(request):
    return JsonResponse({
        "message": "Welcome to Shoes API 🚀",
        "endpoints": {
            "all_shoes": "/shoes/",
            "admin": "/admin/"
        }
    })